/* =======================
   RESET DE MÁRGENES Y PADDINGS
   ======================= */
/* Establece márgenes y padding a 0 para todos los elementos */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box; /* Asegura que el padding y el border no afecten al tamaño de los elementos */
}

/* =======================
   ESTILOS PARA EL CUERPO DE LA PÁGINA (Archivo común para todos los archivos HTML)
   ======================= */
body {
    background-color: #111; /* Fondo oscuro para un estilo visual coherente */
    display: flex;
    justify-content: center; /* Centra horizontalmente todos los elementos dentro del body */
    width: 100vw; /* Asegura que el body ocupe todo el ancho de la pantalla */
    font-family: 'Arial', sans-serif; /* Fuente predeterminada para el cuerpo */
    scroll-behavior: smooth; /* Desplazamiento suave cuando se hace scroll */
    flex-direction: column; /* Apila los elementos en columna */
    align-items: center; /* Centra todos los elementos */
}

/* =======================
   ESTILOS PARA EL TÍTULO DEL CAPÍTULO (Se aplica solo a la página de capítulos)
   ======================= */
h1 {
    font-family: 'Bebas Neue', 'Impact', sans-serif; /* Fuente similar al estilo de Solo Leveling */
    font-size: 4rem; /* Aumenta el tamaño para que sea más visible */
    color: #ffcc00; /* Color dorado para el título */
    margin-top: 30px; /* Espaciado superior para separar el título de la imagen */
    margin-bottom: 20px; /* Espaciado inferior */
    text-shadow: 4px 4px 10px black; /* Sombra más pronunciada para darle más impacto */
    letter-spacing: 2px; /* Espaciado entre letras para mayor legibilidad */
    text-align: center; /* Asegura que el título esté centrado */
}

/* =======================
   ESTILOS PARA LOS CAPÍTULOS (Se aplica a las páginas de los capítulos como 'capitulo-1/index.html')
   ======================= */

/* Contenedor de las imágenes del webtoon */
.webtoon {
    display: flex;
    flex-direction: column;
    align-items: center; /* Asegura que las imágenes estén centradas */
    width: 100%;
    max-width: 800px; /* Ajusta el tamaño máximo del webtoon */
    margin-bottom: 60px; /* Espacio al final */
}

/* Estilo de cada imagen dentro del webtoon */
.webtoon img {
    width: 100%;  /* Las imágenes ocuparán el 100% del contenedor */
    height: auto;  /* Mantiene la proporción de las imágenes */
    display: block;
    margin: 0 auto;  /* Asegura que las imágenes estén centradas */
}

/* =======================
   ESTILOS PARA EL BOTÓN DE "VOLVER ARRIBA" (Se aplica en la página de capítulos)
   ======================= */
#scrollTopBtn {
    display: none; /* Inicialmente oculto */
    position: fixed;
    bottom: 20px; /* Posición en la parte inferior de la pantalla */
    right: 20px; /* Posición en la parte derecha */
    background-color: #ffcc00; /* Fondo dorado */
    color: black; /* Texto negro */
    border: none;
    padding: 10px 15px;
    font-size: 20px;
    cursor: pointer;
    border-radius: 50%; /* Botón redondo */
    transition: opacity 0.3s ease, transform 0.3s ease; /* Animación de transición */
    opacity: 0; /* Por defecto no es visible */
}

/* Animación para el botón cuando se despliega */
#scrollTopBtn.show {
    display: block;
    opacity: 1; /* Muestra el botón gradualmente */
    transform: translateY(0); /* El botón se mueve desde la parte inferior */
}

/* Efecto hover sobre el botón */
#scrollTopBtn:hover {
    background-color: #ffaa00; /* Cambio de color al pasar el mouse */
}

/* =======================
   ESTILOS PARA EL ENLACE "VOLVER AL INICIO" (Se aplica en la página de los capítulos)
   ======================= */
.back-link {
    text-decoration: none;
    color: #ffcc00; /* Color dorado para el enlace */
    font-size: 1.2rem; /* Tamaño de fuente */
    display: inline-block;
    margin-top: 20px;
    transition: color 0.3s ease; /* Efecto de transición cuando se pasa el mouse */
}

.back-link:hover {
    color: #ffaa00; /* Cambio de color al pasar el mouse */
}

/* =======================
   ESTILOS PARA LA LISTA DE CAPÍTULOS (Página de lista de capítulos como 'lista-capitulos.html')
   ======================= */

/* Contenedor principal de la lista de capítulos */
.chapter-list {
    display: flex;
    flex-wrap: wrap; /* Permite que las tarjetas se acomoden en varias filas */
    justify-content: center; /* Centra los capítulos horizontalmente */
    gap: 20px; /* Espacio entre cada tarjeta */
    margin-top: 20px;
    padding: 0 10px; /* Relleno para los lados */
}

/* Estilo de cada tarjeta de capítulo */
.chapter-card {
    width: 200px;
    height: 300px;
    background-color: #333; /* Fondo oscuro para cada tarjeta */
    color: white; /* Texto blanco */
    text-align: center;
    border-radius: 8px; /* Bordes redondeados */
    padding: 10px;
    box-shadow: 0 4px 8px rgba(255, 255, 255, 0.2); /* Sombra sutil */
    text-decoration: none; /* Sin subrayado en el enlace */
    transition: 0.3s; /* Transición suave al pasar el mouse */
}

/* Imagen dentro de la tarjeta */
.chapter-card img {
    width: 100%;
    height: 180px; /* Altura fija para las imágenes */
    object-fit: cover; /* Asegura que las imágenes cubran el espacio sin distorsionarse */
    border-radius: 5px; /* Bordes redondeados para la imagen */
}

/* Título dentro de la tarjeta */
.chapter-card h3 {
    margin-top: 10px;
    font-size: 1.2rem; /* Tamaño de la fuente */
    font-weight: bold; /* Hace el texto más destacado */
    letter-spacing: 1px; /* Espaciado entre letras */
}

/* Efecto de hover sobre la tarjeta */
.chapter-card:hover {
    transform: scale(1.05); /* Aumenta el tamaño de la tarjeta al pasar el mouse */
    box-shadow: 0 8px 16px rgba(255, 255, 255, 0.3); /* Aumenta la sombra para mayor profundidad */
}